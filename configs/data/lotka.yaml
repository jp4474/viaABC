# conf/data/lotka.yaml - Most common working version
_target_: src.data.datamodule.SimulationDataModule
data_dir: ${paths.data_dir}/lotka
transform: null
# ...
dataset:
  _target_: src.data.components.dataset.LotkaVolterraDataset
  data_dir: ${..data_dir}    # Simple peer reference
  prefix: train 
  transform: ${..transform}